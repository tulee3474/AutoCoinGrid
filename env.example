# docker-compose.prod.yml의 mysql 서비스 자격증명 — 이 파일을 .env로 복사해서 값을 채우세요
# (docker compose가 같은 디렉토리의 .env를 자동으로 읽어 ${..} 치환에 사용, .env 자체는 git에 안 올라감)
# 사용법: cp env.example .env

MYSQL_ROOT_PASSWORD=
MYSQL_DATABASE=autocoin
MYSQL_USER=autocoin
MYSQL_PASSWORD=
